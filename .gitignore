node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
*.psd

# Internal docs (security audits, deploy notes) — not committed
docs/internal/

# Credentials — never commit. Secrets live in GCP Secret Manager.
*serviceaccount*.json
*service-account*.json
*adminsdk*.json
*-key.json
*.pem

# Python (voice-agent/)
.venv/
__pycache__/
*.pyc
.pytest_cache/
