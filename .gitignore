node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
!.env.demo
*.psd

# Internal docs (security audits, deploy notes) — not committed
docs/internal/

# Next.js
.next/
next-env.d.ts
.e2e-shots/
*.tsbuildinfo

# Temporary QA / test scratch scripts (Playwright probes, one-off checks)
*.tmp.cjs

# Backups written by scripts/set-hero.mjs when swapping the hero image
.hero-backups/
